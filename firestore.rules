rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * FIRESTORE SECURITY RULES - SIMON STYLES LIMITED
     * 
     * This configuration allows anonymous visitors to:
     * 1. Read blog posts and their statistics.
     * 2. Read and list comments/replies.
     * 3. Add new comments and likes.
     * 4. Update counters (likeCount, commentCount) and existing comments.
     *
     * Admin actions (create/delete posts) are restricted.
     */

    // --- HELPER FUNCTIONS ---

    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }

    // --- COLLECTION RULES ---

    /**
     * @path /blogPosts/{blogPostId}
     * Publicly readable. Updates allowed for counters (likes/comments).
     */
    match /blogPosts/{blogPostId} {
      allow read: if true;
      allow update: if true; 
      allow create, delete: if isAdmin();

      /**
       * @path /blogPosts/{blogPostId}/comments/{commentId}
       * Publicly readable and listable for community engagement.
       */
      match /comments/{commentId} {
        // Explicitly allow both 'get' and 'list' via 'read'
        allow read, list: if true;
        // Allow anonymous users to post comments
        allow create: if true;
        // Allow anonymous updates (e.g., incrementing comment likeCount)
        allow update: if true;
        allow delete: if isAdmin();

        /**
         * @path /blogPosts/{blogPostId}/comments/{commentId}/likes/{likeId}
         */
        match /likes/{likeId} {
          allow read, list, create: if true;
          allow delete: if isAdmin();
        }
      }

      /**
       * @path /blogPosts/{blogPostId}/likes/{likeId}
       */
      match /likes/{likeId} {
        allow read, list, create: if true;
        allow delete: if isAdmin();
      }
    }

    // Admin collection for verification
    match /admins/{userId} {
      allow get: if true;
    }

    // --- FALLBACK ---
    match /{path=**} {
      allow read, write: if isAdmin();
    }
  }
}