
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * FIRESTORE SECURITY RULES - SIMON STYLES LIMITED
     * 
     * ENFORCING LEAST PRIVILEGE:
     * 1. Public can read blog posts and stock status.
     * 2. Only authenticated Admins can modify content.
     * 3. Community features (likes/comments) are enabled for anonymous users.
     */

    // --- HELPER FUNCTIONS ---

    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }

    // --- COLLECTION RULES ---

    /**
     * @path /blogPosts/{blogPostId}
     * Publicly readable. Updates restricted to Admins.
     */
    match /blogPosts/{blogPostId} {
      allow read: if true;
      allow write: if isAdmin();

      match /comments/{commentId} {
        allow read: if true;
        allow create: if true;
        allow update, delete: if isAdmin();
      }
      
      match /likes/{likeId} {
        allow read: if true;
        allow create: if true;
        allow update, delete: if isAdmin();
      }
    }

    /**
     * @path /laptops_available/{stockId}
     * Live stock status collection.
     */
    match /laptops_available/{stockId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    /**
     * @path /admins/{userId}
     * Admin verification collection. Secured against public get.
     */
    match /admins/{userId} {
      allow get: if isAdmin();
    }

    // --- FALLBACK ---
    match /{path=**} {
      allow read, write: if isAdmin();
    }
  }
}
